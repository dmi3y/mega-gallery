<li class="mg-item">
    <div class="mg-item-teaser">
        <h2 class="mg-item-header">{{teaser.header}}</h2>
        <div class="mg-item-text">
            {{teaser.text}}
        </div>
    </div>
    <div class="mg-item-image">
        <img src="{{image.src}}" alt="{{image.alt}}" />
    </div>
</li>
